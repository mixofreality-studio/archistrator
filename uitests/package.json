{
  "name": "archistrator-uitests",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Black-box, browser-driven end-to-end tests for the archistrator SPA. The UI sibling of ../systemtests: it links ZERO webApp source and drives the real running SPA over the wire, selecting only by published data-testid.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "list": "playwright test --list",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^25.0.3",
    "typescript": "~5.9.3"
  }
}
