node_modules/
dist/
.DS_Store
server/server
server/cmd/server/server
.npmrc
server/.env

# local research material (e.g. the Righting Software book) — not committed.
# Anchored to the repo root: .aiarch/state/research/ is committed project state (F42 corpus files).
/research/
.mcp-pilot/
