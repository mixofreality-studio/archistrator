# appgen Step 3: modelgen → framework-go-app-generator

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Checkbox steps.

**Goal:** Move the contract.gen.go emitter into the platform (`framework-go-app-generator/modelgen`) so delivered projects (gtdapp) can generate their model layer; `server/cmd/modelgen` becomes a thin shim. Close the gen-models drift-gate gap found in recon.

**Spec:** docs/superpowers/specs/2026-07-06-app-generator-codegen-design.md, delivery step 3.

## Global Constraints

- **Byte-identity is the gate:** archistrator's 28 committed `contract.gen.go` files must regenerate with ZERO diff through the platform library. Therefore this is a FAITHFUL PORT of `server/cmd/modelgen/main.go` (1177 lines), parameterizing only: module path (emitManagerConstructor's `serverModulePath`), the engine-impl allowlist (8 archistrator keys), and the input path (driver concern). The emitted header string `// Code generated by cmd/modelgen from contract.schema.json; DO NOT EDIT.` stays VERBATIM (load-bearing provenance; cosmetic rename is an earmark). Raw-JSON property-order recovery (`propOrder`/`orderedKeys`) ports untouched.
- **Descriptor types move:** `cmd/internal/codegen`'s 5 types (Interface/Operation/Param/SumType/SumVariant) become exported types of the platform `modelgen` package; `schemagen` (bootstrap tool, stays in-repo) re-imports them from the platform; `cmd/internal/codegen` is deleted. No other cmd consumes it (recon-verified).
- **NOT in scope (earmarks):** consuming projectmodel types inside modelgen (projectmodel.Doc lacks structured $defs access — needs a projectmodel extension first); making the infra-bindings registry extensible; header-string modernization.
- Branches `appgen-step3` (platform off main; archistrator off qa-gtd-pass_rearch). Platform merge → tag `framework-go-app-generator/v0.2.0` → push (standing authorization from step 2). Archistrator merges to qa-gtd-pass_rearch, stays unpushed.
- Per-task gates as in steps 1-2 (GOWORK=off test/vet/lint/gofmt; archistrator task adds app boot + Chrome).

### Task 1 (platform): port the emitter as a library
**Files:** create framework-go-app-generator/modelgen/{modelgen.go,descriptors.go,…} (split the 1177-line file into lint-passing units: parsing, defs/enum/sum emitters, interface/impl emitters, infra registry, naming/util — funlen 60/40 + gocyclo/gocognit 10 apply; the port may split functions but MUST NOT change emission logic); modify framework-go-app-generator/go.mod (+ github.com/google/jsonschema-go).
- [ ] Library API: `type Config struct { ModulePath string; EngineImplAllowlist []string }`, `func Generate(projectJSON []byte, cfg Config) (map[string][]byte, error)` — key = the contract's goPackage, value = the formatted contract.gen.go bytes; entries without goPackage skipped. Descriptor types exported (moved verbatim from cmd/internal/codegen with the jsonschema-go dependency).
- [ ] Tests: (a) greenfield golden — Generate over testdata/greenfield.project.json, golden-compare the three emitted files (hand-verify them on first -update against the rules); (b) archistrator-fixture smoke — Generate over framework-go-projectmodel's testdata/archistrator.project.json (copy it into this module's testdata or read via relative path consistent with module standalone builds — COPY it) with archistrator's ModulePath + 8-key allowlist: assert 28 outputs? (count = entries with goPackage), every output go/parser-parses, and BYTE-COMPARE at least the systemdesign manager + projectstate RA outputs against the REAL committed files snapshotted into testdata (2 golden snapshots — proves the port's fidelity inside the platform's own CI).
- [ ] Gates; commit `modelgen: port the contract.gen.go emitter from archistrator (library form, parameterized module path + engine allowlist)`.

### Task 1b (controller): review, merge platform → main, tag+push `framework-go-app-generator/v0.2.0`.

### Task 2 (archistrator): shim + descriptor re-import + pin + drift gate
**Files:** rewrite server/cmd/modelgen/main.go as a shim (read project.json → platform `modelgen.Generate` with `ModulePath: "github.com/mixofreality-studio/archistrator/server"` + the 8-key allowlist → write files, preserving CLI arg + default path behavior); delete server/cmd/internal/codegen/; modify server/cmd/schemagen/main.go imports (codegen.X → platform modelgen.X); server/go.mod (app-generator → v0.2.0); server/Makefile (add `gen-models-check`: gen-models + `git diff --exit-code -- 'internal/**/contract.gen.go'`); .github/workflows server drift step extended to also run gen-models-check.
- [ ] `make gen-models` → `git status` MUST show zero changes across all 28 contract.gen.go files (the byte-identity gate; any diff = port bug → BLOCKED, fix in platform).
- [ ] Full gates (`GOWORK=off go build ./... && go test -short ./... && go vet ./...`, lint touched dirs) + `make gen-models-check gen-temporal-check`.
- [ ] Commit `appgen: modelgen shim over platform library v0.2.0; codegen descriptors moved; gen-models drift gate`.

### Task 2b (controller): review, boot + Chrome gate, merge appgen-step3 → qa-gtd-pass_rearch, update spec Step-3 outcome line + memory, wrap.
