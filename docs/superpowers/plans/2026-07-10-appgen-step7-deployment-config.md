# appgen Step 7: Deployment Model + config.gen.go

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Checkbox steps.

**Goal:** Extend `operationalConcepts.deployment` with typed `infrastructure`/`bindings`/`settings`; projectmodel parses + validates them; a new `configgen` emitter produces `config.gen.go` (env parsing, profile selection, boot validation, dormant warnings) that REPLACES `cmd/server/config.go`'s hand `loadConfig`; a generated harness env helper replaces systemtests' hand boot-env duplication. `run()`/main stays hand-written until step 8.

**Ground truth (verified):** deployment today = deliveryStyle + containers[archistrator-server, archistrator-webapp] + environments[cloud, test]. The server's real profile axes live in cmd/server/config.go + main.go (git-local vs github; CONSTRUCTION_DRYRUN; OPERATIONS_DRYRUN; dev auth) — the amendment must model THOSE, adding a `local` environment. Migration path precedent: surgical validate-gated splice of project.json (structurizr Task 8), gated by cmd/validate + the methoddesign workspace test + projectmodel.Load + the projectmodelguard test.

## Global Constraints

- Schema evolution is platform-first (spec's ownership section): projectmodel gains tolerant parsing + validation FIRST, tagged, then the archistrator document gains the data. Absent new sections ⇒ zero behavior change for every existing document (gtdapp etc.).
- ENV COMPATIBILITY IS ABSOLUTE for archistrator: every existing `ARCHISTRATOR_*` var keeps its exact name/semantics via explicit `env:` overrides in the bindings/settings/infrastructure entries. `config.gen.go` must reproduce cmd/server/config.go's behavior for identical env input — required-var errors, defaults, dev-mode implications. The swap is proven by the boot rig + live systemtests slice.
- The substrate catalog (mechanism) lives in the generator; project.json declares intent only (spec §deployment). Catalog entries needed now: temporal, postgres, github-app, keycloak, otel + the profile-scalar settings machinery.
- methodcheck (framework-go) must stay green — new deployment fields are additive; verify its typed decode tolerates them (it does by Go-default unknown-field tolerance; confirm with the workspace-mode methoddesign test).
- Branches `appgen-step7` both repos; platform tags at merge (projectmodel/v0.2.0, app-generator/v0.4.0); push per standing authorization.

### Task 1 (platform): projectmodel deployment parsing + validation
framework-go-projectmodel/deployment.go: parse `operationalConcepts.deployment` (located via slots kind==6) into typed Deployment{DeliveryStyle, Containers, Environments, Infrastructure []InfraDecl{Key, Substrate, Profiles, Presence, Env overrides map}, Bindings []Binding{Component, PerProfile map[profile]BindingVariant{Variant, Infra []infraKey}, Presence, Provides []string, Settings []Setting}, Settings []Setting{Name, Type, Default, Env, Description}} — tolerant (all new sections optional). Validation (only when sections present): binding components resolve to contracts; infra keys resolve; profile keys ⊆ environments' profiles; presence enum; provides entries resolve to contracts. Broken fixtures + archistrator fixture (which today has NO new sections → parses with empty slices — pin that). Model.Deployment surfaced. Gates + commit.

### Task 2 (platform): configgen emitter
framework-go-app-generator/configgen: `Generate(m, Config{ContainerKey, ModulePath, EnvPrefix}) (map[string][]byte)` emitting `config.gen.go` for a container: a Config struct with one field per (infrastructure input × settings), env parsing via a catalog (substrate catalog file in configgen: temporal{HOSTPORT,NAMESPACE}, postgres{URL}, github-app{APP_ID,PRIVATE_KEY_PEM,ACCOUNT,APP_SLUG,INSTALLATION_ID,API_BASE_URL}, keycloak{JWKS_URL,ISSUER}, otel{} (env-driven by OTEL_* convention, no vars), each input with default env name `<PREFIX>_<KEY>_<INPUT>` overridable per-declaration), profile selection var (default `<PREFIX>_PROFILE` with legacy-toggle overrides expressible — see Task 3 note), boot-time validation collecting ALL missing required vars for the selected profile, per-dormant-optional warn strings, and typed accessors the hand run() consumes. Greenfield golden + gates + commit. NOTE: study cmd/server/config.go FIRST — the emitter's feature set must cover what that file actually does (bool parsing, durations, dev-mode struct, conditional requirements) or the archistrator adoption (Task 4) fails; enumerate any config.go behavior the model can't express and STOP if one is structural (report, don't hack).

### Task 3 (archistrator): the amendment splice
Scripted splice (python/go, committed as a one-off under scripts/ or done inline and recorded in the report) adding to slot-6 deployment: a `local` environment (dev profile); `infrastructure` decls (temporal all profiles; postgres cloud+local; github-app cloud optional; keycloak cloud optional; otel all) with env overrides matching TODAY'S names (ARCHISTRATOR_TEMPORAL_HOSTPORT etc.); `bindings` for the RA components per profile from main.go's real construction (projectStateAccess: local→GitLocal/cloud→GitHub; artifactAccess/constructionPipelineAccess/sourceControlAccess: cloud-optional-dormant + dryrun variants where main.go has them; operatedRuntimeAccess local-vs-real; operatedSystemStateAccess+usageAccess postgres; billing stubs) with presence semantics mirroring main.go's nil-tolerance, `provides` for the git-store multi-contract case; `settings` (escalation timeout, intervention mode, repo owner/name/workflow/ref, listen addr, shutdown timeout, dev auth, dryrun toggles as legacy-env settings). Profile selection: keep TODAY'S toggle vars as the source (no new ARCHISTRATOR_PROFILE requirement yet — model it as settings the generated config exposes; the profile RESOLUTION stays a hand run() concern until step 8 — configgen just parses + validates vars per its declarations). Gates: cmd/validate green; methoddesign workspace test green; projectmodel.Load green (new sections validate); projectmodelguard test green; commit.

### Task 4 (archistrator): adopt config.gen.go in cmd/server + harness helper
- cmd/appgen extended: configgen for container archistrator-server → server/cmd/server/config.gen.go; Makefile gen-config + gen-config-check + CI fold.
- cmd/server: hand config.go DELETED; loadConfig callers move to the generated struct/loader (a thin hand adapter file may bridge naming so main.go/run() edits stay minimal — keep honest: enumerate every behavioral difference; identical-env-identical-behavior is the gate).
- systemtests harness env helper: generate (same appgen run) a small env-declaration file into systemtests/internal/sdk or internal/harness/gen — the harness's StartServer/Infra consume the generated var-name constants instead of hand strings (recon step-5 found ~130L duplication).
- Gates: server build/test-short/vet; ALL gen-checks; BOOT RIG full check (controller: healthz + all 5 workers + Chrome, all profiles exercisable locally: default local-git boot + dryrun toggles); live systemtests UC1 slice; commit(s).

### Wrap: reviews per task as usual; platform merge+tags; archistrator merge → main; push both; spec Step-7 outcome; memory/ledger.
