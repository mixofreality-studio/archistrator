# appgen Step 4: Typed OAS (RC1 + RC2)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Checkbox steps.

**Goal:** The OAS carries real slot-model schemas (RC1) and the webApp consumes generated enum tables (RC2) — deleting `models.ts` (468 L), regenerating `enums.ts`, and thinning `wire.ts` ~40%. HONEST SCOPE (recon 2026-07-09): `wire.ts` survives thinned (PascalCase→camelCase view renaming is orthogonal); display-order/label data stays hand-authored product data; uitests untouched (wire ordinals unchanged).

**Branch:** `appgen-step4` off qa-gtd-pass_rearch (archistrator only — platform untouched; httpgen already passes `$defs`+`x-enum-varnames` through verbatim).

## Global Constraints

- **Wire compatibility is absolute:** no enum ordinal changes, no envelope shape changes, no contract (project.json) changes. The manager contracts keep `model: json.RawMessage` — typing is an OAS-read concern, spliced at clientgen merge time. NEVER add slot models to serviceContracts `$defs` (modelgen would emit duplicate structs — they are `bindByName`'d deliberately).
- Slot-model schemas are REFLECTED from the Go structs at gen-client time (jsonschema-go `ForType`, schemagen's proven technique) — Go stays the authority per the spec's schema-ownership section.
- Gates per task: `npm run check`-equivalents that are green today (tsc via `npx tsc --noEmit -p webApp/tsconfig.app.json`, eslint, vite build — prettier drift is pre-existing/not enforced), server `GOWORK=off go build/test -short/vet`, gen byte-idempotency (`make gen-client` twice → stable), and for UI-visible tasks: boot + real Chrome check (controller) + the uitests playwright suite (`cd uitests && npx playwright test` — check the repo's documented invocation; report infra-gated skips honestly).
- webApp layering (eslint-boundaries: routes→components→hooks→api + contracts/utilities leaves) must stay green.

### Task 1 (server): reflect slot-model schemas into the merged OAS + repoint DraftModel
**Files:** create server/cmd/clientgen/modelschemas.go (+ small test); modify server/cmd/clientgen/main.go (mergeOpenAPI splice); regen server/api/openapi.yaml.
- [ ] `modelschemas.go`: reflect the 17 ArtifactModel variants (MissionStatement, Glossary, ScrubbedRequirements, Volatilities, CoreUseCases, System, OperationalConcepts, StandardCheck, PlanningAssumptions, ActivityList, Network, Solution [shared by the 4 solution kinds], RiskModel, SdpReview — enumerate from projectstate.AllArtifactKinds/NewModelForKind) via jsonschema-go into named-`$defs` schemas, handling the recursive types (ActivityNode, DeploymentNode) with `$ref` cycles — study cmd/schemagen's walker for the proven approach and REUSE its technique (copy the minimal needed helpers; do not import schemagen). Strip x-go-* extensions (TS doesn't need them). Namespace under prefix `Model` (e.g. `ModelMissionStatement`).
- [ ] Splice into `mergeOpenAPI` output as a shared component block (relax the "no cross-contract $ref" assumption ONLY for the `Model*` namespace); repoint the OAS-side (post-merge patch) `SystemDesignDraftModel.model`, `ProjectDesignDraftModel.model`, and the project-envelope slot model fields from `type:["null"]` to `oneOf` over the variant refs (find every `model` field that is the opaque raw-message in the merged doc — grep the yaml for the x-go-type json.RawMessage pattern and enumerate; each gets the SAME oneOf).
- [ ] Determinism: sorted emission; `make gen-client` twice → byte-stable openapi.yaml. Existing REST handler/MCP outputs must be UNCHANGED (the splice touches only the merged OAS document).
- [ ] Gates + commit `clientgen: typed OAS — slot-model schemas reflected into components, DraftModel.model oneOf (RC1)`.

### Task 2 (webApp): generated enum tables + regen schema.ts
**Files:** modify webApp/scripts/gen-api.mjs; create (generated) webApp/src/contracts/enums.gen.ts; regen webApp/src/contracts/schema.ts.
- [ ] Extend gen-api.mjs: after reading the OAS, extract every component schema carrying `x-enum-varnames` → emit `src/contracts/enums.gen.ts` (header `// Code generated by gen-api. DO NOT EDIT.`) containing, per enum: a string-literal union type of the varnames (normalized: strip Go const prefixes to the wire-name style the hand enums.ts uses — study enums.ts's existing name mapping, e.g. KindMission→"mission"; if the varname→app-string mapping is NOT mechanically derivable for some enums, emit the GO CONST NAMES verbatim and let Task 3 keep a tiny hand mapping for those — report which), ordinal→name array/map, name→ordinal map. Deterministic output, prettier-formatted like schema.ts.
- [ ] Regen schema.ts (now carries `Model*` schemas + typed `model` unions). Assert in the report: `SystemDesignDraftModel.model` is no longer `null` in schema.ts, and `ModelMissionStatement` exists.
- [ ] Gates (tsc/eslint/vite build still green — hand files untouched this task) + commit `webApp: generated enum tables from x-enum-varnames + typed schema.ts (RC2 groundwork)`.

### Task 3 (webApp): re-point the enum boundary (Stage B)
**Files:** wire.ts + the 6 hooks (useSessionState, useProjectSessionState, useDesignMutations, useProjectDesignMutations, useConstructionMutations, useOperationsMutations); shrink/delete enums.ts.
- [ ] Swap every `…FromOrdinal`/`…ToOrdinal`/const-map import from enums.ts to enums.gen.ts equivalents. Behavior identical (the hand tables and generated tables must agree — add a TEMPORARY test or one-off script comparing them before deleting, and record the diff-check in the report). Delete enums.ts if fully covered; if Task 2 left non-derivable name mappings, keep ONLY those in a slim hand module with a comment.
- [ ] Gates + Chrome boot check (controller) + commit `webApp: enum boundary onto generated tables; hand ordinal tables deleted`.

### Task 4 (webApp): models onto generated types (Stage C — the big one)
**Files:** types.ts (re-point its re-exports to schema.ts `Model*` types), adapters.ts / projectAdapters.ts / constructionAdapters.ts, the ~7 one-symbol leaf component imports, operationsTypes.ts; DELETE models.ts.
- [ ] Mechanism: types.ts currently re-exports models.ts types — re-point those aliases to `components["schemas"]["Model…"]` types from schema.ts (type aliases keep consumer import paths stable). Fix shape drift where the generated types differ from the hand mirrors (optionality/nullability differences from Go omitempty — the hand models.ts matched Go json tags exactly, so drift should be small; enumerate every drift in the report). mapEnvelope/mapProjectEnvelope collapse to typed narrowing (kind-keyed) instead of `as` casts.
- [ ] Delete models.ts once tsc is green across all consumers. NO runtime behavior changes — this is a type-layer swap; any place where a REAL shape mismatch surfaces (not just typing) → STOP and report (could be a live decode bug the hand types papered over).
- [ ] Gates + playwright uitests + Chrome boot check + commit `webApp: slot-model types from typed OAS; models.ts deleted (RC1 consumer cut)`.

### Task 5 (webApp): thin wire.ts + consolidate artifact-kind lists (Stage D)
- [ ] Excise dead ordinal calls from the ~8 mixed mappers; drop mapEnvelope/mapProjectEnvelope if fully collapsed in Task 4. KEEP rename/defaulting/composition mappers (mapSlot, mapServiceContract, reviewStatus trio, etc.).
- [ ] Consolidate: fold PHASE1_ARTIFACTS/PHASE2_DRAFTABLE_ARTIFACTS + ARTIFACT_LABELS/PROJECT_ARTIFACT_LABELS into METHOD_METADATA (add order/phase fields; display order remains hand-authored — it legitimately differs from wire ordinal order). Update the ~4 list consumers.
- [ ] Gates + playwright + Chrome + commit `webApp: wire.ts thinned (enum calls + opaque casts gone); artifact-kind lists consolidated into METHOD_METADATA`.

### Wrap (controller): review-lite whole-step, spec Step-4 outcome subsection (honest reframe recorded), merge → qa-gtd-pass_rearch, memory, ledger.
