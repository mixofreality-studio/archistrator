// Code generated by framework-go-http-generator. DO NOT EDIT.
// Source contract:

package delivery

import (
	"encoding/json"
	"errors"
	"net/http"

	fwmanager "github.com/mixofreality-studio/archistrator-platform/framework-go/manager"
	"github.com/mixofreality-studio/archistrator-platform/framework-go/utilities/security"
	mgr "github.com/mixofreality-studio/archistrator/server/internal/manager/delivery"
)

// Handler binds the DeliveryManager manager to HTTP per the archistrator client convention.
type Handler struct {
	Manager  mgr.DeliveryManager
	Security security.Security
}

// Register mounts every operation route on mux.
func (h *Handler) Register(mux *http.ServeMux) {
	mux.HandleFunc("POST /api/v1/delivery/start-project", h.handleStartProject)
	mux.HandleFunc("POST /api/v1/delivery/execute-next-activity/{projectID}", h.handleExecuteNextActivity)
	mux.HandleFunc("POST /api/v1/delivery/dispatch-activity-task/{projectID}/{activityID}", h.handleDispatchActivityTask)
	mux.HandleFunc("POST /api/v1/delivery/submit-review-decision/{projectID}/{activityID}", h.handleSubmitReviewDecision)
	mux.HandleFunc("POST /api/v1/delivery/ask-questions/{projectID}/{activityID}", h.handleAskQuestions)
	mux.HandleFunc("POST /api/v1/delivery/acknowledge-stale-basis/{projectID}/{activityID}", h.handleAcknowledgeStaleBasis)
	mux.HandleFunc("POST /api/v1/delivery/set-project-run-state/{projectID}", h.handleSetProjectRunState)
	mux.HandleFunc("POST /api/v1/delivery/override-activity/{projectID}/{activityID}", h.handleOverrideActivity)
	mux.HandleFunc("POST /api/v1/delivery/replan-project/{projectID}", h.handleReplanProject)
	mux.HandleFunc("POST /api/v1/delivery/set-project-execution-policy/{projectID}", h.handleSetProjectExecutionPolicy)
	mux.HandleFunc("POST /api/v1/delivery/query-project-view", h.handleQueryProjectView)
	mux.HandleFunc("GET /api/v1/delivery/query-activity-view/{projectID}/{activityID}", h.handleQueryActivityView)
}

type startProjectRequest struct {
	Owner     mgr.OwnerScope      `json:"owner"`
	Name      string              `json:"name"`
	ProjectID *string             `json:"projectID"`
	Model     *mgr.OperatingModel `json:"model"`
	Research  *mgr.ResearchInput  `json:"research"`
	Start     bool                `json:"start"`
}

type executeNextActivityRequest struct {
	TickID string `json:"tickID"`
}

type dispatchActivityTaskRequest struct {
	TaskID   string              `json:"taskID"`
	Feedback *mgr.ReviewFeedback `json:"feedback"`
}

type submitReviewDecisionRequest struct {
	TaskID   string                  `json:"taskID"`
	Decision mgr.ReviewDecisionInput `json:"decision"`
	Feedback *mgr.ReviewFeedback     `json:"feedback"`
}

type askQuestionsRequest struct {
	TaskID    string                `json:"taskID"`
	Addressee string                `json:"addressee"`
	Questions []mgr.AnchoredComment `json:"questions"`
}

type acknowledgeStaleBasisRequest struct {
	TaskID string `json:"taskID"`
	Note   string `json:"note"`
}

type setProjectRunStateRequest struct {
	RunState mgr.ProjectRunState `json:"runState"`
	Reason   string              `json:"reason"`
}

type overrideActivityRequest struct {
	Override mgr.ActivityOverride `json:"override"`
}

type replanProjectRequest struct {
	TickID string `json:"tickID"`
}

type setProjectExecutionPolicyRequest struct {
	Policy mgr.ExecutionPolicyInput `json:"policy"`
}

type queryProjectViewRequest struct {
	Query mgr.ProjectViewQuery `json:"query"`
}

// handleStartProject binds POST /api/v1/delivery/start-project -> mgr.StartProject.
func (h *Handler) handleStartProject(w http.ResponseWriter, r *http.Request) {
	var req startProjectRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "start-project"},
		security.ResourceRef{Kind: "deliveryCatalog", ID: principal.Subject})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	result, err := h.Manager.StartProject(rc, req.Owner, req.Name, req.ProjectID, req.Model, req.Research, req.Start)
	if err != nil {
		writeManagerError(w, err)
		return
	}
	writeJSON(w, http.StatusOK, result)
}

// handleExecuteNextActivity binds POST /api/v1/delivery/execute-next-activity/{projectID} -> mgr.ExecuteNextActivity.
func (h *Handler) handleExecuteNextActivity(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	var req executeNextActivityRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "execute-next-activity"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	result, err := h.Manager.ExecuteNextActivity(rc, projectID, req.TickID)
	if err != nil {
		writeManagerError(w, err)
		return
	}
	writeJSON(w, http.StatusOK, result)
}

// handleDispatchActivityTask binds POST /api/v1/delivery/dispatch-activity-task/{projectID}/{activityID} -> mgr.DispatchActivityTask.
func (h *Handler) handleDispatchActivityTask(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	activityID := mgr.ActivityID(r.PathValue("activityID"))
	var req dispatchActivityTaskRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "dispatch-activity-task"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	result, err := h.Manager.DispatchActivityTask(rc, projectID, activityID, req.TaskID, req.Feedback)
	if err != nil {
		writeManagerError(w, err)
		return
	}
	writeJSON(w, http.StatusOK, result)
}

// handleSubmitReviewDecision binds POST /api/v1/delivery/submit-review-decision/{projectID}/{activityID} -> mgr.SubmitReviewDecision.
func (h *Handler) handleSubmitReviewDecision(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	activityID := mgr.ActivityID(r.PathValue("activityID"))
	var req submitReviewDecisionRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "submit-review-decision"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	if err := h.Manager.SubmitReviewDecision(rc, projectID, activityID, req.TaskID, req.Decision, req.Feedback); err != nil {
		writeManagerError(w, err)
		return
	}
	w.WriteHeader(http.StatusNoContent)
}

// handleAskQuestions binds POST /api/v1/delivery/ask-questions/{projectID}/{activityID} -> mgr.AskQuestions.
func (h *Handler) handleAskQuestions(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	activityID := mgr.ActivityID(r.PathValue("activityID"))
	var req askQuestionsRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "ask-questions"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	if err := h.Manager.AskQuestions(rc, projectID, activityID, req.TaskID, req.Addressee, req.Questions); err != nil {
		writeManagerError(w, err)
		return
	}
	w.WriteHeader(http.StatusNoContent)
}

// handleAcknowledgeStaleBasis binds POST /api/v1/delivery/acknowledge-stale-basis/{projectID}/{activityID} -> mgr.AcknowledgeStaleBasis.
func (h *Handler) handleAcknowledgeStaleBasis(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	activityID := mgr.ActivityID(r.PathValue("activityID"))
	var req acknowledgeStaleBasisRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "acknowledge-stale-basis"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	if err := h.Manager.AcknowledgeStaleBasis(rc, projectID, activityID, req.TaskID, req.Note); err != nil {
		writeManagerError(w, err)
		return
	}
	w.WriteHeader(http.StatusNoContent)
}

// handleSetProjectRunState binds POST /api/v1/delivery/set-project-run-state/{projectID} -> mgr.SetProjectRunState.
func (h *Handler) handleSetProjectRunState(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	var req setProjectRunStateRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "set-project-run-state"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	if err := h.Manager.SetProjectRunState(rc, projectID, req.RunState, req.Reason); err != nil {
		writeManagerError(w, err)
		return
	}
	w.WriteHeader(http.StatusNoContent)
}

// handleOverrideActivity binds POST /api/v1/delivery/override-activity/{projectID}/{activityID} -> mgr.OverrideActivity.
func (h *Handler) handleOverrideActivity(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	activityID := mgr.ActivityID(r.PathValue("activityID"))
	var req overrideActivityRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "override-activity"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	if err := h.Manager.OverrideActivity(rc, projectID, activityID, req.Override); err != nil {
		writeManagerError(w, err)
		return
	}
	w.WriteHeader(http.StatusNoContent)
}

// handleReplanProject binds POST /api/v1/delivery/replan-project/{projectID} -> mgr.ReplanProject.
func (h *Handler) handleReplanProject(w http.ResponseWriter, r *http.Request) {
	projectIDVal := mgr.ProjectID(r.PathValue("projectID"))
	projectID := &projectIDVal
	var req replanProjectRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "replan-project"},
		security.ResourceRef{Kind: "project", ID: string(*projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	result, err := h.Manager.ReplanProject(rc, projectID, req.TickID)
	if err != nil {
		writeManagerError(w, err)
		return
	}
	writeJSON(w, http.StatusOK, result)
}

// handleSetProjectExecutionPolicy binds POST /api/v1/delivery/set-project-execution-policy/{projectID} -> mgr.SetProjectExecutionPolicy.
func (h *Handler) handleSetProjectExecutionPolicy(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	var req setProjectExecutionPolicyRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "set-project-execution-policy"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	if err := h.Manager.SetProjectExecutionPolicy(rc, projectID, req.Policy); err != nil {
		writeManagerError(w, err)
		return
	}
	w.WriteHeader(http.StatusNoContent)
}

// handleQueryProjectView binds POST /api/v1/delivery/query-project-view -> mgr.QueryProjectView.
func (h *Handler) handleQueryProjectView(w http.ResponseWriter, r *http.Request) {
	var req queryProjectViewRequest
	if !decodeJSON(w, r, &req) {
		return
	}
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "query-project-view"},
		security.ResourceRef{Kind: "deliveryCatalog", ID: principal.Subject})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	result, err := h.Manager.QueryProjectView(rc, req.Query)
	if err != nil {
		writeManagerError(w, err)
		return
	}
	writeJSON(w, http.StatusOK, result)
}

// handleQueryActivityView binds GET /api/v1/delivery/query-activity-view/{projectID}/{activityID} -> mgr.QueryActivityView.
func (h *Handler) handleQueryActivityView(w http.ResponseWriter, r *http.Request) {
	projectID := mgr.ProjectID(r.PathValue("projectID"))
	activityID := mgr.ActivityID(r.PathValue("activityID"))
	principal, ok := security.PrincipalFrom(r.Context())
	if !ok {
		writeError(w, http.StatusUnauthorized, "unauthenticated", "authentication required")
		return
	}
	decision, err := h.Security.Authorize(r.Context(), principal,
		security.Action{Verb: "query-activity-view"},
		security.ResourceRef{Kind: "project", ID: string(projectID)})
	if err != nil || !decision.Permit {
		writeError(w, http.StatusForbidden, "forbidden", "not permitted")
		return
	}
	rc := fwmanager.Context{Context: r.Context(), Principal: principal}
	result, err := h.Manager.QueryActivityView(rc, projectID, activityID)
	if err != nil {
		writeManagerError(w, err)
		return
	}
	writeJSON(w, http.StatusOK, result)
}

// --- response helpers ------------------------------------------------------

type errorResponse struct {
	Error string `json:"error"`
	Code  string `json:"code"`
}

func decodeJSON(w http.ResponseWriter, r *http.Request, dst any) bool {
	dec := json.NewDecoder(r.Body)
	dec.DisallowUnknownFields()
	if err := dec.Decode(dst); err != nil {
		writeError(w, http.StatusBadRequest, "bad_request", "invalid JSON body: "+err.Error())
		return false
	}
	return true
}

func writeJSON(w http.ResponseWriter, status int, body any) {
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(status)
	_ = json.NewEncoder(w).Encode(body)
}

func writeError(w http.ResponseWriter, status int, code, detail string) {
	writeJSON(w, status, errorResponse{Error: detail, Code: code})
}

// writeManagerError maps a framework-go manager.Error to its HTTP status; any
// other error is a non-leaking 500.
func writeManagerError(w http.ResponseWriter, err error) {
	var me *fwmanager.Error
	if errors.As(err, &me) {
		writeError(w, statusForKind(me.Kind), codeForKind(me.Kind), me.Detail)
		return
	}
	writeError(w, http.StatusInternalServerError, "internal", "internal error")
}

func statusForKind(kind fwmanager.Kind) int {
	switch kind {
	case fwmanager.ContractMisuse:
		return http.StatusBadRequest
	case fwmanager.NotFound:
		return http.StatusNotFound
	case fwmanager.Unauthorized:
		return http.StatusForbidden
	case fwmanager.FailedPrecondition:
		return http.StatusConflict
	case fwmanager.Infrastructure:
		return http.StatusServiceUnavailable
	default:
		return http.StatusInternalServerError
	}
}

func codeForKind(kind fwmanager.Kind) string {
	switch kind {
	case fwmanager.ContractMisuse:
		return "contract_misuse"
	case fwmanager.NotFound:
		return "not_found"
	case fwmanager.Unauthorized:
		return "forbidden"
	case fwmanager.FailedPrecondition:
		return "failed_precondition"
	case fwmanager.Infrastructure:
		return "infrastructure"
	default:
		return "internal"
	}
}
